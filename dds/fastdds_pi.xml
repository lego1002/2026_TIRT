<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Fast DDS profile —— 樹莓派(Pi)端。PC 端請看同目錄的 fastdds_pc.xml。
  由 dds/setup_dds.sh 依「本機是否持有 10.77.0.2」自動挑選,不要手動指定。

  ※ 這裡的 IP 是寫死的常數,對應 net/tirt_net.conf 的 TIRT_PI_IP / TIRT_PC_IP。
    要換網段時兩邊都要改(XML 沒辦法讀 shell 變數)。
    以前這個檔是含 @LAN_IP@ 佔位符的範本,每次換場地要用 sed 重新渲染;
    改用固定別名 IP 之後就不需要了 —— 這正是簡化的重點。

  這份設定做兩件事,缺一不可:

  (1) 發現走 Fast DDS Discovery Server(純 unicast),不靠 multicast。
      為什麼:實測場地的 WiFi AP 不轉發 client 之間的 multicast(`ros2 multicast
      send`/`receive` 對測收不到,但 ping 通)。而 DDS 預設的自動發現(SPDP)靠 multicast,
      所以兩台永遠互相看不見 —— `ros2 topic list` 甚至連對方 topic 都沒有。改成對固定的
      discovery server 做 unicast 發現就繞開了。server 由 Pi 端的 tmux `dds` 視窗
      (pi/robot_tmux.sh → dds/run_discovery_server.sh)持有。
      RemoteServer 的 prefix 44.53.00.5f... 是 `fastdds discovery -i 0`(server-id 0)的
      固定 GUID prefix,不要改。

      **為什麼是 SUPER_CLIENT 而不是 CLIENT(2026-07-26 實測確認)**:
      Discovery Server 只會把「和你自己端點有配對」的發現資訊送給 CLIENT。
      `ros2 topic list` / `node list` 自己沒有訂閱任何 topic,所以 server 什麼都不給它 ——
      結果就是在 Pi 上跑 CLI 只看得到自己創的 /parameter_events 和 /rosout,
      即使同一個 shell 裡用純 rclpy 訂閱 /odom 可以正常收到 62 筆訊息。
      這曾被誤判成「CLI 內省在這個架構下就是壞的、不可修」,寫進了 CLAUDE.md。
      實際上 SUPER_CLIENT 會拿到 server 的**完整**發現資料庫,CLI 就正常了:
      改成 SUPER_CLIENT 後 `ros2 topic list` 立刻列出全部 11 個 topic。
      代價是每個 participant 會收到全部發現流量 —— 這個規模的系統完全可忽略。

  (2) 資料只走固定別名 IP(interfaceWhiteList = 10.77.0.2 + localhost),
      排除 tailscale0 / docker0 / DHCP 位址。
      為什麼:Pi/PC 都同時有 wlan0 + tailscale0(+ docker0)。就算發現接上了,若不綁位址,
      /robot_description、/tf、/map 這類大樣本可能被走 tailscale(MTU 1280)傳 → 分片掉包 →
      RViz 收不到 TF、畫面空白。綁死別名就解決,而且順便讓兩台的通訊路徑完全確定。
-->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
    <profiles>
        <transport_descriptors>
            <transport_descriptor>
                <transport_id>tirt_transport</transport_id>
                <type>UDPv4</type>
                <interfaceWhiteList>
                    <address>10.77.0.2</address>
                    <address>127.0.0.1</address>
                </interfaceWhiteList>
            </transport_descriptor>
        </transport_descriptors>
        <participant profile_name="tirt_participant" is_default_profile="true">
            <rtps>
                <userTransports>
                    <transport_id>tirt_transport</transport_id>
                </userTransports>
                <useBuiltinTransports>false</useBuiltinTransports>
                <builtin>
                    <discovery_config>
                        <discoveryProtocol>SUPER_CLIENT</discoveryProtocol>
                        <discoveryServersList>
                            <RemoteServer prefix="44.53.00.5f.45.50.52.4f.53.49.4d.41">
                                <metatrafficUnicastLocatorList>
                                    <locator>
                                        <udpv4>
                                            <address>10.77.0.2</address>
                                            <port>11811</port>
                                        </udpv4>
                                    </locator>
                                </metatrafficUnicastLocatorList>
                            </RemoteServer>
                        </discoveryServersList>
                    </discovery_config>
                </builtin>
            </rtps>
        </participant>
    </profiles>
</dds>
